<UserControl x:Class="Hymperia.Facade.Views.Reglages.Editeur.AccesProjet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Hymperia.Facade.Views.Reglages.Editeur"
    xmlns:prop="clr-namespace:Hymperia.Facade.Properties">
  <StackPanel Orientation="Vertical">
    <TextBlock Text="" />
    <DataGrid x:Name="DataGridAccesProjet" AutoGenerateColumns="True" CanUserAddRows="True" IsReadOnly="False">
      <DataGridTemplateColumn />

      <DataGrid.Columns>
        <DataGridTextColumn Header="{x:Static prop:Resources._Utilisateur}" Binding="{Binding .}" MinWidth="150"/>
        <DataGridCheckBoxColumn Header="Droit édition" MinWidth="150"/>
        <DataGridTemplateColumn Header="{x:Static prop:Resources.Supprimer}" MinWidth="150">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Button Content="{x:Static prop:Resources.Supprimer}" />
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>
  </StackPanel>
</UserControl>
